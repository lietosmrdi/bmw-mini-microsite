{
  "name": "bmw-mini-microsite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^2.2.0",
    "normalize.css": "^4.2.0",
    "remodal": "^1.1.0"
  },
  "devDependencies": {
    "node-sass": "^3.8.0"
  },
  "scripts": {
    "sass": "node-sass -o src/css src/css/main.scss -w",
    "server": "live-server --open=src",
    "dev": "concurrently \"npm run server\" \"npm run sass\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
